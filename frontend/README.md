# Emotion Detection Chatbot Frontend

React + Vite + Tailwind UI for the MoodMate chatbot.

```bash
npm install
cp .env.example .env
npm run dev
```

The app expects the FastAPI backend at `VITE_API_URL`, defaulting to `http://localhost:8000`.
